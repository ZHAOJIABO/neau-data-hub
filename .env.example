# Docker compose data directory mounted into ruoyi-backend-pg as /app/data.
# On the production server this points to the existing uploaded TIF/table data.
NEAU_DATA_DIR=/root/workspace/neau-data-hub/data
